
// export const PORT = process?.env?.PORT ?? '8080';
// export const HOST = process?.env?.HOST ?? '0.0.0.0';
export const PORT = '8080';
export const HOST = 'localhost';

export const API_SERVER_URL = `http://${HOST}:${PORT}`;